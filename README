
GETTING STARTED:

Requirements:
	* C++11 basic development toolset:
		* GNU gcc preferred
		* libstdc++

	* Boost (tokenizer, lexical_cast, property_tree (json_parser))
	* libircclient >= 1.8
	* LevelDB


Compile:
	1. type `make`
