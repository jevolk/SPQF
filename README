
GETTING STARTED:

Requirements:
	* Development toolset:
		* C++11 for libircbot   (tested: g++ 4.9.1)
		* C++14 for SPQF        (tested: g++ 4.9.1)
		* libstdc++             (tested: 4.9)

	* Boost:                    (tested: 1.54)
		* tokenizer
		* lexical_cast
		* property_tree, json_parser
		* locale (linkage required)

	* libircclient >= 1.8 (linkage required)

	* LevelDB (linkage required)


Compile:
	1. type `make`
	2. type `make` again if necessary
